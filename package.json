{
  "name": "codestack",
  "version": "0.1.0",
  "private": true,
  "description": "CodeStack — LeetCode-style coding-education platform (NestJS API + React UI)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.3",
  "scripts": {
    "dev:api": "pnpm --filter @codestack/api start:dev",
    "dev:web": "pnpm --filter @codestack/web dev",
    "build": "pnpm -r build",
    "build:api": "pnpm --filter @codestack/api build",
    "build:web": "pnpm --filter @codestack/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @codestack/api test:e2e",
    "seed": "pnpm --filter @codestack/api seed",
    "migration:run": "pnpm --filter @codestack/api migration:run"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "argon2",
      "@nestjs/core"
    ]
  }
}
